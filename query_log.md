```sql
CREATE TABLE IF NOT EXISTS CarsDB (Car TEXT, MPG REAL, Cylinders INTEGER, Displacement REAL, Horsepower REAL, Weight REAL, Acceleration REAL, Model INTEGER, Origin TEXT);
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
SELECT * FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE CarsDB SET MPG=20.0, Cylinders=6 WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM CarsDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS CarsDB (Car TEXT, MPG REAL, Cylinders INTEGER, Displacement REAL, Horsepower REAL, Weight REAL, Acceleration REAL, Model INTEGER, Origin TEXT);
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
SELECT * FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE CarsDB SET MPG=20.0, Cylinders=6 WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM CarsDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS CarsDB (Car TEXT, MPG REAL, Cylinders INTEGER, Displacement REAL, Horsepower REAL, Weight REAL, Acceleration REAL, Model INTEGER, Origin TEXT);
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
SELECT * FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE CarsDB SET MPG=20.0, Cylinders=6 WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM CarsDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS CarsDB (Car TEXT, MPG REAL, Cylinders INTEGER, Displacement REAL, Horsepower REAL, Weight REAL, Acceleration REAL, Model INTEGER, Origin TEXT);
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
SELECT * FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE CarsDB SET MPG=20.0, Cylinders=6 WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM CarsDB LIMIT 5;
```

```sql
CREATE TABLE IF NOT EXISTS CarsDB (Car TEXT, MPG REAL, Cylinders INTEGER, Displacement REAL, Horsepower REAL, Weight REAL, Acceleration REAL, Model INTEGER, Origin TEXT);
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
INSERT INTO CarsDB (Car, MPG, Cylinders, Displacement, Horsepower, Weight, Acceleration, Model, Origin) VALUES ('Example Car', 25.0, 4, 150.0, 100.0, 2500.0, 10.0, 2023, 'US');
```

```sql
SELECT * FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
UPDATE CarsDB SET MPG=20.0, Cylinders=6 WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
DELETE FROM CarsDB WHERE Car = 'Chevrolet Chevelle Malibu';
```

```sql
SELECT * FROM CarsDB LIMIT 5;
```

